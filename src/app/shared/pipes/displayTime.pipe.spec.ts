import { DisplayTimePipe } from './displayTime.pipe';

describe('DisplayTimePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayTimePipe();
    expect(pipe).toBeTruthy();
  });
});
